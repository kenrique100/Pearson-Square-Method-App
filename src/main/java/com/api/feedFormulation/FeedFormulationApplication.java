package com.api.feedFormulation;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The entry point of the Feed Calculator Spring Boot application.
 * This class contains the main method that starts the application.
 */
@SpringBootApplication
public class FeedFormulationApplication {

	public static void main(String[] args) {
		SpringApplication.run(FeedFormulationApplication.class, args);
	}
}
