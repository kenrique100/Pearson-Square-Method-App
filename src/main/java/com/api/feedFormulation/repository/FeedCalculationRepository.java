/*
package com.api.feedFormulation.repository;

import com.api.feedFormulation.model.FeedCalculation;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.Date;
import java.util.List;

@Repository
public interface FeedCalculationRepository extends JpaRepository<FeedCalculation, String> {
    List<FeedCalculation> findByCustomerName(String customerName);
    List<FeedCalculation> findByCreatedDate(Date createdDate);
    List<FeedCalculation> findByCustomerId(String customerGuid);
}
*/
